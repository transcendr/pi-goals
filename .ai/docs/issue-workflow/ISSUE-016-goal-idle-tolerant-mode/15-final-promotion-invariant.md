# 15 — Final promotion invariant

## Probe

`raw/final-promotion-invariant-probe.log`

## Coverage

The probe fails if the open canonical issue is missing, the old refine issue still exists, open status/bucket are missing, the locked idle-nudge/manual policy design is absent, required proof rows are missing, or artifact links `00` through `14` are not present.

## Result

The probe passed.
